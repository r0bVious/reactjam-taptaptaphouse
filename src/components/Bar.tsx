import React from "react";
import Customer from "./Customer";

type Props = {};

const Bar = (props: Props) => {
  return (
    <div className="bar">
      <Customer />
    </div>
  );
};

export default Bar;
